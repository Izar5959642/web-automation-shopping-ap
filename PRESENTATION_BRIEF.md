# מצגת ארכיטקטורה — Web Automation Shopping App

## הוראות לקלוד
צור מצגת של 10 שקפים בעברית על בסיס המידע הבא.
המצגת מיועדת לקהל טכני (סטודנטים + מרצה).
הסגנון צריך להיות פשוט, ברור, ומתאים להצגה בכיתה של 10 דקות.
בכל שקף שיש בו תיאור שכבות או זרימת מידע — צור דיאגרמה ויזואלית עם חיצים.

---

## תוכן המצגת

---

### שקף 1 — כותרת
**כותרת:** Web Automation Shopping App
**תת-כותרת:** אפליקציית קניות אוטומטית מבוססת דפדפן
**טכנולוגיות:** React · Express · Playwright · TypeScript

---

### שקף 2 — מה האפליקציה עושה
**כותרת:** מה האפליקציה עושה?

3 פעולות עיקריות:
1. **חיפוש** — המשתמש מקליד שאילתה, האפליקציה פותחת דפדפן אמיתי, מתחברת לאתר Swag Labs ומחזירה מוצרים
2. **הוספה לעגלה** — המשתמש בוחר מוצרים, מצב העגלה נשמר גם לאחר רענון דף
3. **תשלום** — המשתמש ממלא פרטי משלוח, האפליקציה מבצעת את התשלום ומחזירה צילום מסך של האישור

---

### שקף 3 — הארכיטקטורה: סקירה כללית
**כותרת:** ארכיטקטורה — 5 שכבות

האפליקציה בנויה מ-5 שכבות מסודרות. כל שכבה מדברת רק עם השכבה שמתחתיה — לעולם לא הפוך.

**דיאגרמה (צור ויזואלית עם חיצים מלמעלה למטה):**
```
[ ui ]
   ↓
[ api ]
   ↓
[ services ]
   ↓
[ automation ]
   ↓
[ domain ]
```

הכלל: תלות זורמת כלפי מטה בלבד.

---

### שקף 4 — שכבת UI
**כותרת:** שכבת UI — ממשק המשתמש

- טכנולוגיה: React SPA
- מסכים: חיפוש, תוצאות, עגלה, תשלום, אישור
- ניהול מצב עגלה גלובלי דרך `CartContext`
- העגלה נשמרת ב-`localStorage` — לא נאבדת ברענון

---

### שקף 5 — שכבת API
**כותרת:** שכבת API — שרת Express

- שרת REST API על פורט 3000
- 3 נתיבים: `/api/search`, `/api/buy`, `/api/checkout`
- כל בקשה מקבלת `requestId` ייחודי (UUID) לצורך מעקב
- `TraceCollector` — אוסף את שלבי הביצוע של כל בקשה ומחזיר אותם ל-UI

---

### שקף 6 — שכבת Services
**כותרת:** שכבת Services — לוגיקה עסקית

- `SearchService` — מפעיל חיפוש, מנרמל מחירים (הסרת $, פסיקים), מסנן לפי מחיר מקסימלי
- `PurchaseService` — מנהל עגלה, מחשב סכום כולל, מבצע checkout
- `SelectionPolicy` — בוחר את המוצר הזול ביותר מרשימת תוצאות

---

### שקף 7 — שכבת Automation
**כותרת:** שכבת Automation — אוטומציית דפדפן

- טכנולוגיה: Playwright
- `BaseScraper` — מחלקה מופשטת (abstract) שמגדירה את הממשק
- `SwaglabsScraper` — מימוש ספציפי לאתר saucedemo.com
- סלקטורים מרוכזים בקובץ אחד: `selectors/swaglabs.ts`

**יתרון:** להוסיף תמיכה באתר חדש = רק ליצור מחלקה חדשה שיורשת מ-`BaseScraper`. שאר הקוד לא משתנה.

---

### שקף 8 — שכבת Domain
**כותרת:** שכבת Domain — מודלים משותפים

- טיפוסי TypeScript טהורים — ללא תלויות חיצוניות
- המודלים: `Product`, `RawProduct`, `Cart`, `Order`, `Trace`
- כל שכבה אחרת משתמשת במודלים האלה

---

### שקף 9 — זרימת נתונים: דוגמה מלאה
**כותרת:** זרימת נתונים — חיפוש מוצר

**דיאגרמה (צור ויזואלית עם חיצים):**
```
המשתמש מקליד "backpack"
        ↓
     [ UI ] שולח בקשה ל-API
        ↓
     [ API ] יוצר requestId, קורא ל-SearchService
        ↓
  [ Services ] מנרמל מחירים, מסנן תוצאות
        ↓
 [ Automation ] פותח דפדפן, מתחבר לאתר, מחזיר מוצרים
        ↓
    [ Domain ] מוצרים מוחזרים כ-Product[]
        ↓
     [ UI ] מציג תוצאות למשתמש
```

---

### שקף 10 — סיכום
**כותרת:** סיכום

- ארכיטקטורה של 5 שכבות עם תלות חד-כיוונית
- הפרדה מלאה בין UI, לוגיקה, ואוטומציה
- ניתן להרחיב לאתרים נוספים ללא שינוי בקוד קיים
- מעקב מלא אחרי כל בקשה דרך `requestId` ו-`TraceCollector`
- בדיקות: 18 בדיקות יחידה + בדיקת E2E מלאה
